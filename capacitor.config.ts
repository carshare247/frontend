import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carpool.app',
  appName: 'CarShare247',
  webDir: 'dist/car-pool/browser'
};

export default config;
